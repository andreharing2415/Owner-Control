---
phase: 0
plan: "00-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/requirements.txt
  - server/app/auth.py
  - server/app/routers/subscription.py
  - server/tests/test_auth.py
  - server/tests/test_subscription.py
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "token forjado/expirado é rejeitado com 401"
    - "cancelamento não faz downgrade imediato"
  artifacts:
    - "PyJWT >= 2.8.0 configurado sem python-jose"
    - "fluxo de cancelamento com downgrade no evento terminal"
  key_links:
    - "auth.py usa PyJWT para encode/decode"
    - "subscription webhook aplica downgrade final"
---

<objective>
Fechar INFRA-02 e INFRA-03 migrando autenticação para PyJWT seguro e corrigindo regra de cancelamento Stripe por período.

Purpose: remover risco de CVE em auth e preservar receita/experiência do assinante.
Output: autenticação segura e cancelamento aderente ao ciclo pago.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-bloqueadores-cr-ticos/RESEARCH.md
@.planning/phases/00-bloqueadores-cr-ticos/00-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar dependências JWT para PyJWT</name>
  <files>server/requirements.txt, server/app/auth.py</files>
  <action>Remover python-jose, adicionar PyJWT>=2.8.0 e adaptar encode/decode/exceções mantendo HS256 e contratos HTTP.</action>
  <verify><automated>cd server && rg -n "python-jose|PyJWT" requirements.txt || true; pytest -q tests -k "auth or jwt or token"</automated></verify>
  <done>PyJWT presente em versão mínima, sem python-jose, com login/refresh funcionando e 401 para token inválido.</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir cancelamento sem downgrade imediato</name>
  <files>server/app/routers/subscription.py</files>
  <action>Ajustar cancel_subscription para manter plano pago até o fim do período e mover downgrade para evento terminal.</action>
  <verify><automated>cd server && pytest -q tests -k "subscription and cancel"</automated></verify>
  <done>Cancelamento marca fim de período sem trocar plano para gratuito na requisição inicial.</done>
</task>

<task type="auto">
  <name>Task 3: Cobrir regressões críticas de auth e assinatura</name>
  <files>server/tests/test_auth.py, server/tests/test_subscription.py</files>
  <action>Criar/ajustar testes para token válido/expirado/forjado e estados de assinatura no cancelamento e evento terminal.</action>
  <verify><automated>cd server && pytest -q tests -k "auth or jwt or subscription"</automated></verify>
  <done>Suite da fase verde para auth/subscription sem regressões funcionais.</done>
</task>

</tasks>

<verification>
- INFRA-03 validado por dependência e testes de auth.
- INFRA-02 validado por fluxo temporal de assinatura.
</verification>

<success_criteria>
- Tokens forjados/expirados rejeitados.
- Assinante mantém acesso até o fim do período cobrado.
</success_criteria>

<output>
After completion, create `.planning/phases/00-bloqueadores-cr-ticos/00-02-SUMMARY.md`.
</output>
