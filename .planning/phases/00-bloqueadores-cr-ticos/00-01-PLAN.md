---
phase: 0
plan: "00-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/alembic/versions/20260309_0014_add_valor_realizado.py
  - server/alembic/versions/20260309_0014_checklist_unificado.py
  - server/alembic/versions/20260319_0023_projetodoc_erro_detalhe.py
  - server/alembic/versions/20260319_0024_composite_indexes.py
  - server/deploy-cloudrun.sh
autonomous: true
requirements:
  - INFRA-01
  - INFRA-05
must_haves:
  truths:
    - "alembic upgrade head em banco limpo sem erro"
    - "servico Cloud Run com min instances igual a 1"
  artifacts:
    - "cadeia Alembic linear sem IDs duplicados"
    - "deploy-cloudrun.sh com --min-instances=1"
  key_links:
    - "down_revision coerente entre migrations"
    - "deploy aplica scaling.minInstanceCount=1"
---

<objective>
Fechar INFRA-01 e INFRA-05 com cadeia Alembic válida para deploy fresh e configuração anti-cold-start no Cloud Run.

Purpose: estabilizar base de produção para qualquer execução posterior da fase.
Output: migrations consistentes e deploy script com min instances ativo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-bloqueadores-cr-ticos/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir cadeia Alembic duplicada</name>
  <files>server/alembic/versions/20260309_0014_add_valor_realizado.py, server/alembic/versions/20260309_0014_checklist_unificado.py, server/alembic/versions/20260319_0023_projetodoc_erro_detalhe.py, server/alembic/versions/20260319_0024_composite_indexes.py</files>
  <action>Normalizar revision IDs para valores unicos e ajustar down_revision para cadeia linear sem bifurcacao.</action>
  <verify><automated>cd server && python -m alembic heads && python -m alembic history --verbose</automated></verify>
  <done>Comando heads retorna apenas 1 head e history sem referência quebrada.</done>
</task>

<task type="auto">
  <name>Task 2: Validar migration chain em banco limpo</name>
  <files>server/alembic.ini</files>
  <action>Executar upgrade completo em banco limpo para provar deploy fresh funcional.</action>
  <verify><automated>cd server && python -m alembic upgrade head</automated></verify>
  <done>Upgrade conclui sem erro de duplicate revision ou broken down_revision.</done>
</task>

<task type="auto">
  <name>Task 3: Ativar min-instances no deploy Cloud Run</name>
  <files>server/deploy-cloudrun.sh</files>
  <action>Adicionar --min-instances=1 no comando de deploy e validar a configuração aplicada no serviço.</action>
  <verify><automated>gcloud run services describe mestreobra-backend --region us-central1 --project mestreobra --format="value(spec.template.scaling.minInstanceCount)"</automated></verify>
  <done>Valor de minInstanceCount igual a 1 e protocolo de latência pós-idle com p95 < 2s registrado.</done>
</task>

</tasks>

<verification>
- INFRA-01 validado por heads/history/upgrade sem erro.
- INFRA-05 validado por configuração ativa e medição reproduzível.
</verification>

<success_criteria>
- Cadeia Alembic executa em ambiente limpo sem falha.
- Serviço Cloud Run mantém 1 instância mínima.
</success_criteria>

<output>
After completion, create `.planning/phases/00-bloqueadores-cr-ticos/00-01-SUMMARY.md`.
</output>
