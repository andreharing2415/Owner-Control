---
phase: 0
plan: "00-03"
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - server/app/pdf.py
  - server/app/routers/obras.py
  - server/requirements.txt
  - server/tests/test_pdf.py
autonomous: true
requirements:
  - INFRA-04
must_haves:
  truths:
    - "PDF exportado preserva caracteres PT-BR sem corrupção"
  artifacts:
    - "renderer com suporte Unicode real"
    - "teste automatizado com corpus de acentuação"
  key_links:
    - "endpoint de exportação usa novo renderer"
---

<objective>
Fechar INFRA-04 substituindo geração de PDF para suportar UTF-8/português corretamente, preservando contrato da API.

Purpose: eliminar corrupção de texto em relatórios e evidências de obra.
Output: PDF com acentuação correta em produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-bloqueadores-cr-ticos/RESEARCH.md
@.planning/phases/00-bloqueadores-cr-ticos/00-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar renderer PDF com Unicode</name>
  <files>server/app/pdf.py, server/requirements.txt</files>
  <action>Substituir estratégia atual por renderer com suporte Unicode (WeasyPrint + Jinja2), removendo conversão latin1 com replace.</action>
  <verify><automated>cd server && rg -n "weasyprint|jinja2|latin1" app/pdf.py requirements.txt</automated></verify>
  <done>Pipeline de geração não usa fallback de latin1 que corrompe acentuação.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar renderer ao endpoint de exportação</name>
  <files>server/app/routers/obras.py, server/app/pdf.py</files>
  <action>Adaptar endpoint para utilizar o novo renderer mantendo status, headers e contrato de download.</action>
  <verify><automated>cd server && pytest -q tests -k "pdf and obras"</automated></verify>
  <done>Endpoint continua funcional e retorna PDF válido no mesmo contrato da API.</done>
</task>

<task type="auto">
  <name>Task 3: Validar corpus PT-BR sem corrupção</name>
  <files>server/tests/test_pdf.py</files>
  <action>Adicionar teste com caracteres ã, ç, é, ê, õ, á, í, ó, ú e falhar se houver substituição por caracteres inválidos.</action>
  <verify><automated>cd server && pytest -q tests -k "pdf"</automated></verify>
  <done>Teste garante legibilidade do corpus PT-BR no PDF gerado.</done>
</task>

</tasks>

<verification>
- INFRA-04 validado por testes e smoke de exportação.
</verification>

<success_criteria>
- PDFs não exibem "?" ou quadrados no lugar de acentos em português.
</success_criteria>

<output>
After completion, create `.planning/phases/00-bloqueadores-cr-ticos/00-03-SUMMARY.md`.
</output>
